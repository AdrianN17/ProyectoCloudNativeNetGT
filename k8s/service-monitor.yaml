apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: apis-monitor
  namespace: monitoring
  labels:
    release: prometheus
spec:
  namespaceSelector:
    matchNames:
      - apis
  selector:
    matchLabels: {}
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
